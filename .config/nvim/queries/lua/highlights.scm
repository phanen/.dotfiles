;; extends

; TODO: only use for dotfiles

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "vim"))

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "u"))

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "api"))

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "fn"))

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "fs"))

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "lsp"))

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "map"))

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "autocmd"))

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "cmd"))
